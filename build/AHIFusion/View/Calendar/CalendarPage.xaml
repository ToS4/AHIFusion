<Page x:ConnectionId='1'
    x:Class="AHIFusion.CalendarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AHIFusion"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"                     >

  <Page.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="ms-appx:///AHIFusion/Styles/ColorPaletteOverride.xaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Page.Resources>
  
  <Grid>

    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="15*"/>
      <ColumnDefinition Width="70*"/>
      <ColumnDefinition Width="15*"/>
    </Grid.ColumnDefinitions>

    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid Grid.Column="0" Background="{ThemeResource SecondaryColor}">
      <Grid.RowDefinitions>
        <RowDefinition Height="50*"/>
        <RowDefinition Height="100*"/>
      </Grid.RowDefinitions>

      <Viewbox Stretch="Fill" VerticalAlignment="Top" Margin="4">
        <CalendarView x:ConnectionId='8' x:Name="SmallCalendarView" SelectionMode="Single" CalendarIdentifier="GregorianCalendar"                                                               />
      </Viewbox>

      <Rectangle Fill="{ThemeResource SurfaceColor}" Grid.Row="2"/>
      <StackPanel Grid.Row="2" Margin="5" VerticalAlignment="Bottom">
        <Button x:ConnectionId='6' Content="Save" HorizontalAlignment="Stretch" Margin="5" CornerRadius="10" x:Name="SaveButton"                          />
        <Button x:ConnectionId='7' Content="Load" HorizontalAlignment="Stretch" Margin="5" CornerRadius="10" x:Name="LoadButton"                          />
      </StackPanel>
    </Grid>

    <Grid x:ConnectionId='2' x:Name="CalendarGrid" Grid.Column="1" Margin="10">

      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

    </Grid>

    <Grid Grid.Column="2">

      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>


      <Rectangle HorizontalAlignment="Stretch" Fill="{ThemeResource SecondaryColor}"/>
      <TextBlock x:ConnectionId='3' x:Name="CurrentDayDateTextBlock" TextAlignment="Center" FontSize="40" Margin="15" TextWrapping="Wrap" HorizontalAlignment="Stretch"/>

      <StackPanel x:ConnectionId='4' Grid.Row="1" VerticalAlignment="Stretch" x:Name="CurrentDayEventsStackPanel" Background="{ThemeResource SurfaceColor}"/>
      
      <Button x:ConnectionId='5' x:Name="AddButton" CornerRadius="10" Grid.Row="2" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Content="Add Event"                        />
    </Grid>

  </Grid>
</Page>

