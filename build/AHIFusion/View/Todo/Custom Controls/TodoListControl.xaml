<UserControl x:ConnectionId='1'
  x:Class="AHIFusion.TodoListControl"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="using:AHIFusion"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DesignHeight="300"
  d:DesignWidth="400"
  DataContext="{Binding RelativeSource={RelativeSource Self}}">

  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="ms-appx:///AHIFusion/Styles/ColorPaletteOverride.xaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>
  
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="5*"/>
    </Grid.ColumnDefinitions>

    <Border CornerRadius="3" BorderThickness="1" BorderBrush="{StaticResource OutlineColor}" Grid.ColumnSpan="2">
      <Rectangle x:ConnectionId='6' x:Name="BackgroundRect" Fill="{StaticResource SecondaryColor}"/>
    </Border>
    <Viewbox Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center">
      <Grid>
        <TextBlock x:ConnectionId='4' x:Name="NameTextBlock"                                   Margin="2,6,6,6" Foreground="{StaticResource OnSecondaryColor}"/>
        <TextBox x:ConnectionId='5' x:Name="NameTextBox"                                                                             Foreground="{StaticResource OnSecondaryColor}"                                   Margin="2,6,6,6" FontSize="30"/>
      </Grid>
    </Viewbox>

    <Viewbox>
      <Ellipse x:ConnectionId='2' Width="20" Height="20" Margin="12,8,8,8" Stroke="AliceBlue"                                        >
        <Ellipse.Fill>
          <SolidColorBrush x:ConnectionId='3'                                     />
        </Ellipse.Fill>
      </Ellipse>
    </Viewbox>
  </Grid>
  
</UserControl>

